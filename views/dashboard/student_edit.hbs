<div class="container">
    <form action="/admin/dashboard/student-edit/{{ student._id }}" method="post">
        <label for="">Student Name</label>
        <input type="text" id="" name="name" value="{{ student.name }}" required>

        <label for="">Telephone</label>
        <input type="text" id="" name="telephone" value="{{ student.telephone }}" required>

        <label for="">Email</label>
        <input type="text" id="" name="email" value="{{ student.email }}" required>

        <label for="">Subject</label>
        <input type="text" id="" name="subject" value="{{ student.subject }}" required>

        <input type="submit" value="Save">
    </form>
</div>

<style>
    body {
        font-family: Arial, Helvetica, sans-serif;
    }

    * {
        box-sizing: border-box;
    }

    input[type=text],
    select,
    textarea {
        width: 100%;
        padding: 12px;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
        margin-top: 6px;
        margin-bottom: 16px;
        resize: vertical;
    }

    input[type=submit] {
        background-color: #04AA6D;
        color: white;
        padding: 12px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }

    input[type=submit]:hover {
        background-color: #45a049;
    }

    .container {
        border-radius: 5px;
        background-color: #f2f2f2;
        padding: 20px;
    }
</style>